import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const LoginChoiceScreen = props => {

    return (
        <View>
            <Text>Login Choice</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default LoginChoiceScreen;